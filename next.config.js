/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'images.evetech.net',
            'patreon.com'
        ]
    }
}

module.exports = nextConfig
